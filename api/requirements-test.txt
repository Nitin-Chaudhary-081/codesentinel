# Test/lint dependencies for the CodeSentinel API.
# Runtime deps live in requirements.txt (Flask + sync SQLAlchemy).
flask==3.1.3
flask-cors==6.0.5
sqlalchemy==2.0.51
python-jose[cryptography]==3.5.0

# Code analysis / quality
radon==6.0.1

# Testing / linting
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.6.0
